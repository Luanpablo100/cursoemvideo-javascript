function parImpar(n) { // Recebimento do parâmtro = Parâmetro Formal
    if (n%2==0) {
        return "Par!" //Resposta a chamada da função
    } else {
        return "Impar!"
    }
}

console.log(parImpar(4))
//Chamada da função + entre parênteses = Parâmetro real

